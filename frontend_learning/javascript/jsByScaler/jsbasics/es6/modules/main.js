//we import the modules by using require method

const calculator = require('./calc');

calculator.addition(2, 3);
calculator.subtraction(4, 2);
calculator.division(12, 3);
calculator.multiplication(2, 30);