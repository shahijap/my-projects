// In javascript arrays are objects 
// To check whether the array is array we use isArray method
let arr = ['shahi', 'patnam', 23];

let checkArr = Array.isArray(arr); // it returns boolean 
console.log(checkArr);